version: '3.8'
services:
  super-tic-tac-toe:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: super-tic-tac-toe
    ports:
      - ${PANEL_APP_PORT_HTTP}:3000
    restart: always
    networks:
      - 1panel-network
networks:
  1panel-network:
    external: true